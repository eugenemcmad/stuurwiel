# Dev parity with docker-compose (guest). For production use a real user/password and patch this Secret.
apiVersion: v1
kind: Secret
metadata:
  name: stuurwiel-secrets
type: Opaque
stringData:
  RABBIT_URL: "amqp://guest:guest@rabbitmq:5672/"
